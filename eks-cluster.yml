apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
 
metadata:
  name: awshift-eks
  region: eu-west-3
  version: "1.32"
 
nodeGroups:
  - name: awshift-node-xlarge
    instanceType: t3.xlarge
    iam:
      withAddonPolicies:
        autoScaler: true
        cloudWatch: true
    desiredCapacity: 4
    minSize: 1
    maxSize: 10
    volumeSize: 100
    ssh:
      allow: true
      publicKeyName: awshift-keypair